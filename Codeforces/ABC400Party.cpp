#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int a;
    cin >> a;
    if (400%a==0)
    cout<<400/a;
    else
    cout<<-1;
    return 0;
}