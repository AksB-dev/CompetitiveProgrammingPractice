#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n;
    vector<int> arr1(n);
    for (int i = 0;i<n;i++)
    cin >> a[i];
    cin>>m;
    vector<vector<int>> arr2(m, vector<int>(2));
    for (int i = 0; i<m;i++)
    cin >> arr2[i][0] >> arr2[i][1];
    
}